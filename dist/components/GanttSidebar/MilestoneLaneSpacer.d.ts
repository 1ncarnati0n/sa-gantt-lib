import { ColumnConfig } from '../../types';

interface MilestoneLaneSpacerProps {
    columns: ColumnConfig[];
    dragHandleWidth: number;
    totalWidth: number;
}
export declare const MilestoneLaneSpacer: import('react').MemoExoticComponent<({ columns, dragHandleWidth, totalWidth, }: MilestoneLaneSpacerProps) => import("react/jsx-runtime").JSX.Element>;
export {};
